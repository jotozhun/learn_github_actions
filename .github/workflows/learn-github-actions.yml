name: learn-github-actions
run-name: ${{ github.actor }} is learning Github Actions
on: [push]
jobs:
  my_first_job:
    runs-on: ubuntu-latest
    steps:
      - name: My first app
        uses: docker://3.11.2-alpine:3.17
